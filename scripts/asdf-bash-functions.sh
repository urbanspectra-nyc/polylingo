#!/bin/bash

# Maximize consistency between osx and linux + pan-dev-langs when using asdf.
function sourceWebScript() { curl -s $ASDF_BASH_FUNCTIONS_SCRIPT_URL | source asdf-bash-functions.sh }
# Copy line above and add to local script, followed by: $ 

bash # Subshell supports errors sans crashing terminal window.

function distroFlavorVersion() {
}

function gitEtc() {
}

function installedMeta() { # installedUsing; installedAtVersion; configGet; configSet; testedAtVersion # git 
}

function xStamps() { # time + date + unixtime + microtime
}

